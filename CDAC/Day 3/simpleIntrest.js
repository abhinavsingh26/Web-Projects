// A file of JS in Nodejs is called as module.

module.exports = function(num , r , t ) {
    
    si = ((num*r*t)/100);
    console.log(`Simple Intrest of ${num} is : ${si}`)
    console.log(si)
} 

